# Yooooooooo!

一个用实体悠悠球选择任意支点、摆荡并释放惯性来移动的 2D 物理平台原型。

## 当前阶段

第二阶段移动测试。第一阶段“投掷—固定—摆荡—释放”作为对照基线，本阶段只额外验证固定后的有限角加速度是否增加有趣的运动控制，而不是单纯放大机动性。

## 操作

- `A/D` 或左右方向键：地面水平移动（没有普通跳跃）。固定后不用于选择旋转方向。
- 按住鼠标左键：朝鼠标方向 360° 瞄准、蓄力并进入子弹时间。
- 松开鼠标左键：按当前蓄力投掷实体悠悠球。
- 鼠标右键或 `E`：随时收回悠悠球/解除绳索。
- `R`：重置角色位置和速度；坠落到测试区下方也会自动重置。

## 核心机制

悠悠球以连续碰撞检测的实体刚体直线飞行，首次碰撞的实际接触点成为世界支点。`DistanceJoint2D` 维持固定瞬间形成的绳长，不清零角色速度；解除后完整保留释放瞬间的线速度。空中无方向输入时完整保留横向惯性；空中方向控制最多主动加速到普通地面速度，不会削减摆荡或释放得到的超额惯性。瞄准使用无限子弹时间，蓄力使用未缩放时间。

第二阶段实验可在 `YoyoTuning` 中一键关闭。开启时，它只沿玩家已有的绕点旋转方向施加有限角加速度；切向速度接近零时不自行选择方向，并受最大角速度参数限制。

## 运行

1. 使用 Unity `2022.3.62f2c1` 打开仓库。
2. 打开 `Assets/Scenes/PhaseOnePrototype.unity`。
3. 进入 Play Mode。

若目标场景尚未生成，可执行菜单 `Yooooooooo > Build Phase One Prototype Scene`。场景构建只在编辑器内发生；运行时不会创建 UI。

## 已实现

- 物理角色水平移动、重力和无跳跃控制。
- 鼠标 360° 瞄准、未缩放时间蓄力、子弹时间。
- 可调速度的实体悠悠球、连续碰撞检测、超时安全回收。
- 任意普通 Collider2D 的精确碰撞点固定。
- 最大绳长约束、摆荡、立即收回和惯性保留。
- 地面、墙、天花板、平台、宽坑和高台组成的灰盒场景。
- 预置的方向/蓄力 UI、速度与绳索调试信息、Gizmo 之外的绳线显示。
- 集中在 `YoyoTuning` 资产内的核心参数。
- 可独立开关的第二阶段角加速度实验，以及切向速度、旋转方向和角速度调试显示。

## 已知问题

- 第二阶段角加速度参数尚未完成真实 Play Mode A/B 试玩；当前数值只是首轮测试起点。
- 悠悠球固定在动态刚体时仍以世界坐标为支点；第一阶段灰盒只有静态表面。
- 极短绳长、高速角落碰撞、绕支点超过 180° 等边界行为尚未系统验证。
- UI 的瞄准箭头位于屏幕中央，用于原型反馈，不会跟随角色屏幕位置。

## 结构

- `Assets/Scripts`：移动、瞄准/悠悠球状态、弹体、慢动作、调试和重置逻辑。
- `Assets/Editor/PhaseOneSceneBuilder.cs`：编辑期创建灰盒场景和预置 UI。
- `Assets/Settings/YoyoTuning.asset`：集中调参资产，包含第二阶段实验开关、角加速度、角速度上限和启动阈值。
- `Assets/Scenes/PhaseOnePrototype.unity`：第一阶段试玩场景（场景生成后存在）。
- `docs`：实现记录与未来方向。

## 文档索引

- [开发记录](docs/DEVELOPMENT.md)
- [未来方向](docs/FUTURE.md)
- [Agent 约束](AGENT_phase2.md)
