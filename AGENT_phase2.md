# AGENT.md

## 1. 项目目标

这是一个 2D 物理平台跳跃原型。

主角没有普通跳跃能力，核心移动完全围绕“悠悠球”展开：

1. 玩家指定方向并蓄力。
2. 蓄力/瞄准期间进入子弹时间。
3. 悠悠球作为真实实体沿指定方向直线飞出。
4. 悠悠球命中可碰撞表面后，在精确命中点固定。
5. 主角与该固定点形成绳长约束，并围绕支点进行物理运动。
6. 玩家可随时收回悠悠球并解除约束。
7. 主角解除约束后完整保留当前惯性。

核心体验不是“找预设钩点”，而是：

> 玩家自己决定支点在哪里，并利用物理规则改变自己的速度与轨迹。

项目当前进入**第二阶段测试**。

第一阶段已经建立“任意支点固定 → 绳长约束 → 摆荡/圆周运动 → 收回并继承惯性”的基础移动闭环。

第二阶段不改变这一基础规则，而是额外验证一个单独变量：

> 悠悠球固定后，是否应当主动提供有限的角加速度，从而强化“悠悠球本身参与驱动运动”的感觉。

这仍然只是移动原型测试，不追求完整关卡、美术、剧情、战斗或长期成长系统。第二阶段实验结果未确认前，不得把该角加速度机制视为正式设计。

---

## 2. 最高优先级设计原则

### 2.1 规则明确，解法开放

只限制基础能力和世界规则，不限制玩家具体怎么过关。

优先允许玩家尝试：

- 任意角度投掷；
- 任意表面命中；
- 任意精确命中点固定；
- 不同长度和位置的摆荡；
- 提前收回；
- 高速状态下重新投掷；
- 贴墙移动；
- 连续换支点；
- 利用惯性和物理边界做非常规移动。

不要因为“理论上可能破关”就提前封堵。

如果某种玩法只是非常规、很强、很快、很怪，但仍然需要技巧，并且扩展了玩家的解法空间，应优先保留。

只有当某个技巧满足以下特征时，才考虑限制：

- 非常容易执行；
- 几乎无风险；
- 在绝大多数场景下都是最优解；
- 直接让其他移动方式、关卡结构或核心机制失去意义。

判断标准：

> 好的邪道扩展解空间。  
> 坏的漏洞坍缩解空间。

### 2.2 不要提前设计“防邪道系统”

当前阶段禁止主动加入以下内容，除非实际试玩已经证明有必要：

- 特殊不可固定材质；
- 玻璃表面；
- 预设钩点；
- 同一墙面重复固定限制；
- 最小固定距离；
- 人为吸附点；
- 防爬墙规则；
- 子弹时间资源条；
- 悠悠球 CD；
- 复杂体力系统；
- 为了防止玩家绕关而加入的空气墙。

如果真的出现问题，先记录问题和复现方式，再决定是否修。

不要根据假想中的问题提前加补丁。

### 2.3 允许设计师埋隐藏技巧

允许有意保留或设计一些不在教程中明确说明的高级技巧。

这些技巧应：

- 来源于已经公开给玩家的基础规则；
- 不依赖隐藏数值或纯随机；
- 通过观察、实验和练习可以被发现；
- 能明显提升速度、效率或路线自由度；
- 即使玩家不知道，也不影响正常通关。

不要在教程、README 玩家说明或 UI 中主动剧透这些技巧。

开发文档可以记录其存在。

---

## 3. 当前核心机制

### 3.1 主角

- 2D 物理角色。
- 可以正常左右移动。
- 不能普通跳跃。
- 不能通过其他按钮直接获得向上的跳跃冲量。
- 角色应持续受重力影响。
- 尽量保留物理速度，不要频繁直接覆盖 Rigidbody2D.velocity。

不要把角色控制做成传统平台游戏 CharacterController 式的“贴地锁速度”逻辑。

需要优先保证惯性连续性。

---

### 3.2 悠悠球瞄准与蓄力

按住投掷键后：

- 进入瞄准状态；
- 世界进入明显的子弹时间；
- 玩家可以 360° 指定悠悠球投掷方向；
- 持续按住时进行蓄力；
- UI 至少显示当前方向与蓄力程度；
- 松开后投掷悠悠球并退出子弹时间。

第一版中：

- 子弹时间无限使用；
- 不消耗资源；
- 不设置 CD；
- 不做复杂能量管理。

子弹时间的作用是让玩家能在高速移动中思考和选择支点，而不是制造资源压力。

时间缩放应与物理步进正确配合，避免改变运动结果或产生明显不一致。

---

### 3.3 悠悠球飞行

悠悠球必须是实际存在的物理/逻辑实体，而不是瞬时射线抓钩。

要求：

- 从角色位置发射；
- 沿玩家指定方向直线飞出；
- 初速度由蓄力程度决定；
- 命中碰撞表面后停止飞行；
- 命中点就是实际固定点；
- 不吸附到预设 Anchor；
- 不自动修正到“更合理”的位置。

第一版优先保证行为稳定和直观。

不要主动添加：

- 自动追踪；
- 磁吸；
- 曲线修正；
- 最近固定点搜索；
- 预设抓钩点优先级。

如果项目中使用 Rigidbody2D 实现悠悠球飞行，需要避免高速状态下穿墙，并选择合适的 Collision Detection。

---

### 3.4 固定与绳索约束

悠悠球命中表面后：

- 命中点成为支点；
- 支点保持在世界坐标中的准确位置；
- 主角与支点之间建立绳长约束；
- 初始绳长默认取“固定瞬间主角与命中点之间的距离”；
- 主角继续保留固定前已有的速度；
- 绳索约束只改变不符合约束的运动部分，不应粗暴清零角色速度。

目标是让角色自然围绕支点产生摆荡、圆周运动和惯性转换。

优先使用稳定、可解释的物理约束方案。

可以使用 Joint2D 或自定义约束，但必须：

- 行为稳定；
- 释放前后速度连续；
- 不产生明显瞬移；
- 不在固定瞬间随意清空动量；
- 不为“看起来更顺”偷偷加入无法解释的大量额外冲量。

如果必须做手感修正，需要在开发文档中记录。

---

### 3.5 收回悠悠球

玩家可以随时收回悠悠球。

收回时：

- 立即解除角色与支点的约束；
- 主角保留解除瞬间的当前线速度；
- 不额外把主角拉向悠悠球；
- 不自动减速；
- 不自动修正飞行方向；
- 悠悠球返回玩家或重置为可再次投掷状态。

“释放瞬间继承惯性”是核心机制，不能因为实现方便被削弱。

### 3.6 第二阶段实验：固定后的角加速度

这是**第二阶段测试机制**，不是已经确认的正式核心规则。

测试目的：

> 验证在悠悠球固定后，由悠悠球主动提供有限的绕支点角加速度，是否能让移动从“纯钩爪/摆绳”更接近“悠悠球驱动运动”，同时不让机动性失控。

实验规则：

- 仅在悠悠球已经固定、角色与支点存在绳长约束时生效；
- 不直接给角色瞬时角速度，不使用 `angularVelocity = 固定值` 一类硬赋值；
- 在现有物理运动基础上，沿角色当前绕支点的旋转方向提供可调的角加速度；
- 旋转方向优先由当前切向速度决定，不额外加入“按左/右自由选择顺逆时针”的控制；
- 角速度必须存在可调上限，防止持续固定时无限加速；
- 极低或接近零的切向速度下，不要擅自凭空选择旋转方向；如果试玩发现难以启动，先记录，再单独设计启动规则；
- 重力、碰撞、原有惯性和绳索约束仍然正常参与运动；
- 收回悠悠球时，仍然完整保留此刻最终得到的线速度；
- 必须可以一键关闭该实验机制，以便与第一阶段原始手感直接 A/B 对比。

第二阶段禁止顺手加入：

- 主动切换顺/逆时针的额外按键；
- 无限增长的角速度；
- 固定瞬间直接塞一大段旋转速度；
- 为了配合角加速度而修改投掷、固定、收回的基础规则；
- 因为担心机动性过强而提前加入 CD、体力、不可固定表面或空气墙。

如果角加速度导致移动明显失控，先通过参数和试玩记录判断问题来自：

1. 角加速度过大；
2. 最大角速度过高；
3. 绳长与线速度换算导致的极端情况；
4. 连续换支点带来的速度叠加；
5. 机制本身就不适合。

不要在没有对比测试前直接删掉或正式保留。

---

## 4. 子弹时间实现要求

子弹时间是核心操作的一部分，不只是视觉特效。

需要保证：

- 按住瞄准时稳定进入慢速；
- 松开投掷后立刻或快速恢复正常时间；
- Time.timeScale 调整时同步处理 Time.fixedDeltaTime；
- UI 仍能正常响应；
- 输入不会因为时间缩放丢失；
- 慢动作状态和正常状态下的物理结果尽量一致；
- 不因为帧率不同导致蓄力速度变化。

蓄力逻辑优先使用不受 timeScale 影响的时间来源，使实际蓄力体验稳定。

---

## 5. 阶段范围

### 5.1 第一阶段基线

第一阶段只做：

- 一个可移动角色；
- 禁止普通跳跃；
- 悠悠球方向瞄准；
- 蓄力；
- 子弹时间；
- 悠悠球实体直线投掷；
- 任意碰撞表面命中并固定；
- 绳索/支点物理；
- 收回并继承惯性；
- 简单灰盒测试场景；
- 必要的调试 UI；
- 基础重置功能。

第一阶段内容现在作为第二阶段的**对照基线**，不要为了测试角加速度而破坏。

### 5.2 第二阶段测试范围

第二阶段只增加：

- 固定状态下可开关的角加速度实验；
- 可调角加速度；
- 可调最大角速度；
- 对当前切向速度/旋转方向的检测；
- 必要的调试数据显示；
- 第一阶段基线与第二阶段实验版本的 A/B 对比；
- 对不同绳长、不同初速度、不同支点方向下手感的记录。

第二阶段的目标不是“做得更丰富”，而是回答：

> 加入有限角加速度后，悠悠球移动是否更有辨识度、更有操作空间，同时没有把关卡和路径选择直接打穿？

测试场景继续使用现有灰盒，只在确实无法覆盖测试情况时增加最小必要结构。

不要加入：

- 敌人；
- 伤害；
- 战斗；
- 技能树；
- 收集系统；
- 商店；
- 剧情；
- 正式美术；
- 正式动画；
- 复杂声音系统；
- 程序生成；
- 多人联机；
- 关卡选择；
- 排行榜；
- 存档系统。

---

## 6. 手感调试

所有核心参数应集中管理，不要散落在多个脚本里。

至少暴露：

- 地面移动速度；
- 地面加速度；
- 空中控制强度；
- 重力；
- 悠悠球最小投掷速度；
- 悠悠球最大投掷速度；
- 蓄力时间；
- 子弹时间倍率；
- 悠悠球最大飞行时间或安全回收机制；
- 绳索相关参数；
- 必要的阻尼参数；
- 第二阶段角加速度开关；
- 第二阶段角加速度强度；
- 第二阶段最大角速度；
- 用于判断切向方向/极低切向速度的必要阈值。

优先让这些参数可以在 Inspector 中快速调整。

禁止为了“手感更好”直接在多个脚本中写死 magic number。

---

## 7. 架构要求

默认按 Unity 2D 项目实现。

如果仓库已有明确架构，则优先遵循现有结构，不要为了本任务做大规模重构。

推荐职责分离：

- PlayerMovement：基础角色移动和物理状态；
- YoyoController：悠悠球状态机；
- YoyoProjectile：悠悠球飞行与碰撞；
- YoyoAnchor / Constraint：固定点与绳索约束；
- AimController：瞄准、方向和蓄力；
- BulletTimeController：子弹时间；
- GameDebug / Reset：原型调试。

不要把所有逻辑塞进一个几百行 MonoBehaviour。

同时不要为了“架构漂亮”过度抽象。

当前目标是：

> 清楚、可改、方便调参数、方便快速试玩。

C# 脚本可以保留必要注释，尤其是物理计算、时间缩放和特殊手感修正部分。

---

## 8. 调试要求

需要提供基础调试能力。

建议包括：

- 当前角色速度向量；
- 当前速度大小；
- 当前悠悠球状态；
- 当前支点坐标；
- 当前绳长；
- 当前蓄力比例；
- 当前 timeScale；
- 第二阶段角加速度是否启用；
- 当前切向速度；
- 当前绕支点旋转方向；
- 当前角速度或等价调试值；
- 可选的 Gizmo 显示：
  - 投掷方向；
  - 支点；
  - 绳索；
  - 角色速度向量。

调试显示可以通过一个开关统一关闭。

---


## 8.1 UI 与运行时对象约束

不要在运行时为了省事动态创建一堆 UI。

禁止在 `Awake()`、`Start()` 或其他运行时逻辑中批量执行类似：

- `new GameObject("Canvas")`
- 动态创建 Canvas；
- 动态创建 Text / TMP_Text；
- 动态创建 Slider；
- 动态创建 Image；
- 动态拼装调试面板；
- 每次进入 Play Mode 都重新生成一套 UI 层级。

需要存在的 UI 应优先：

- 预先放入场景；
- 或制作成明确的 Prefab；
- 在 Inspector 中绑定引用；
- 由脚本只更新文本、数值、显隐和状态。

调试 UI 也遵循这一原则。

如果只是为了查看速度、支点、绳长等开发信息，优先使用：

- Inspector；
- Gizmos；
- Debug.Log；
- 已存在的调试面板；

而不是运行时创建新的 UI 对象。

只有真正属于玩法本身、且生命周期明确需要动态生成的对象，才允许运行时实例化。

不要为了“自动初始化方便”牺牲场景层级可读性和可编辑性。

---

## 9. 测试重点

实现后优先验证以下场景：

### 基础行为

- 地面静止时向不同方向投掷；
- 不同蓄力程度；
- 命中墙壁；
- 命中天花板；
- 命中平台侧面；
- 悠悠球未命中后正常回收；
- 固定后立即收回；
- 固定后摆荡一段时间再收回。

### 惯性

- 向右高速运动时固定左上方；
- 向右高速运动时固定右上方；
- 下坠时固定侧上方；
- 摆荡最低点释放；
- 摆荡高点释放；
- 高速状态下进入子弹时间重新瞄准。

### 第二阶段：角加速度 A/B 测试

每组测试都至少对比“关闭角加速度”和“开启角加速度”两种结果。

重点验证：

- 地面低速固定后是否会产生合理差异；
- 高速固定后是否只是放大已有运动，而不是突然改变方向；
- 左上、右上、正上方支点的表现；
- 极短绳长下是否瞬间获得过高线速度；
- 极长绳长下是否几乎感觉不到驱动；
- 不同初始切向速度下达到速度上限的时间；
- 连续换支点是否出现无脑速度滚雪球；
- 在相同路线中，角加速度是否让操作更有意思，还是只让通关更容易；
- 是否出现“固定后只需等待加速再释放”的单一最优策略；
- 关闭角加速度后，第一阶段原始手感是否完全恢复。

记录每次测试使用的关键参数，不要只写主观结论。

### 边界

- 高速悠悠球碰撞；
- 极短绳长；
- 极长绳长；
- 角色与支点非常接近；
- 玩家撞墙时仍处于绳索约束；
- 玩家绕支点超过 180°；
- 连续快速投掷和收回；
- 慢动作中进行大量输入。

### 非预期玩法

如果发现：

- 爬墙；
- 连续短挂；
- 奇怪加速；
- 反向甩动；
- 瞬间换向；
- 利用碰撞获得额外速度；
- 极端绳长技巧；
- 可以绕过测试关卡；

不要第一时间修。

先记录：

1. 操作方式；
2. 复现步骤；
3. 难度；
4. 稳定性；
5. 是否有趣；
6. 是否让其他玩法失去意义。

再决定是否处理。

---

## 10. 文档要求

这个项目不允许只提交代码。

每个阶段完成后都必须同步维护文档。

### 10.1 README.md

仓库根目录必须存在 `README.md`。

至少包含：

- 项目名称；
- 一句话玩法介绍；
- 当前开发阶段；
- 核心操作；
- 核心机制；
- 如何运行项目；
- Unity 版本；
- 当前已实现内容；
- 已知问题；
- 当前项目结构简述；
- 文档索引。

README 面向第一次打开仓库的人。

不要把大量开发流水账全部堆在 README。

---

### 10.2 docs/DEVELOPMENT.md

必须创建并持续维护：

`docs/DEVELOPMENT.md`

用于记录实际开发过程。

至少记录：

- 当前实现方案；
- 当前开发阶段（必须明确标注“第二阶段测试”）；
- 第一阶段基线与第二阶段实验的差异；
- 重要技术决策；
- 物理约束方案；
- 子弹时间实现方案；
- 关键参数；
- 已验证行为；
- 已知 bug；
- 尚未解决的问题；
- 手感修改记录；
- 第二阶段角加速度的每轮测试参数与结果；
- 是否保留、调整或放弃该实验机制的依据；
- 实际发现的邪道/非预期玩法；
- 哪些非预期玩法决定保留；
- 哪些问题决定暂时不修，以及原因。

禁止只写“完成悠悠球系统”这种无信息记录。

应记录为什么这样实现。

---

### 10.3 docs/FUTURE.md

必须创建：

`docs/FUTURE.md`

用于记录未来方向，但不能因为写进 FUTURE 就顺手实现。

可以包含：

- 后续可能增加的关卡元素；
- 可能的高级技巧；
- 可能的隐藏邪道；
- 可能的视觉反馈；
- 可能的音效方向；
- 关卡设计想法；
- 正式美术方向；
- 是否需要挑战关；
- 是否需要计时/速通模式；
- 是否需要排行榜；
- 是否需要回放；
- 需要进一步验证的设计问题。

所有未来功能都应标记为：

- 想法；
- 待验证；
- 已确认；
- 暂缓；
- 放弃。

不要把 FUTURE 当任务清单自动实现。

---

## 11. 开发流程

每次进行较大修改时：

1. 先阅读当前 README 和 docs。
2. 查看已有代码结构。
3. 明确本次修改解决什么问题。
4. 尽量只修改与目标直接相关的部分。
5. 实现。
6. 进行基础编译/静态检查。
7. 如果环境允许，进入 Unity Play Mode 实际测试。
8. 记录结果。
9. 更新 README 或 docs。
10. 再开始下一项。

如果无法运行 Unity，不得声称已经完成 Play Mode 验证。

必须明确区分：

- 已编译；
- 已静态检查；
- 已在编辑器中运行；
- 已实际试玩验证。

---

## 12. 禁止事项

禁止：

- 运行时批量创建 Canvas、Text、Slider、Image 等 UI 对象；

- 擅自把任意固定改成预设钩点；
- 擅自加入不可固定材质；
- 擅自加入跳跃；
- 擅自加入冲刺；
- 擅自加入抓钩自动吸附；
- 擅自加入子弹时间能量条；
- 擅自加入敌人和战斗；
- 擅自增加大型系统；
- 因为担心玩家绕关就添加空气墙；
- 为了修一个局部问题大规模重构整个项目；
- 为了“代码优雅”引入不必要的框架；
- 把核心参数写死在脚本内部；
- 删除或弱化释放后保留惯性的机制；
- 把悠悠球改成瞬时射线抓钩；
- 为了让动画更顺而偷偷篡改核心物理结果。

---

## 13. 第一阶段完成标准（第二阶段对照基线）

第一阶段只有在以下条件全部满足后才算完成：

- 主角不能普通跳跃；
- 主角可正常左右移动；
- 可进入子弹时间瞄准；
- 可 360° 指定悠悠球方向；
- 可蓄力并影响悠悠球投掷速度；
- 悠悠球以实体形式直线飞行；
- 悠悠球可命中任意普通碰撞表面；
- 命中精确位置成为支点；
- 主角围绕支点产生自然物理运动；
- 收回后主角保留当前惯性；
- 高速状态下仍可重新瞄准和投掷；
- 存在一个可测试多种运动方式的灰盒场景；
- 核心参数可调；
- 基础调试信息可查看；
- 没有为了防止假想漏洞加入额外限制；
- `README.md` 已创建并准确反映当前项目；
- `docs/DEVELOPMENT.md` 已创建并记录技术与试玩结果；
- `docs/FUTURE.md` 已创建并记录后续方向；
- 已明确列出尚未完成和未验证的内容。

---

## 14. 第二阶段测试完成标准

第二阶段只有在以下内容完成后，才允许对“角加速度机制是否保留”做结论：

- 第一阶段全部基础行为仍然正常；
- 角加速度实验可以独立开关；
- 关闭实验时能够恢复第一阶段基线手感；
- 角加速度不是通过固定瞬间硬塞角速度实现；
- 角加速度和最大角速度均可集中调参；
- 极低切向速度不会被随意指定旋转方向；
- 收回后仍完整继承当前线速度；
- 已完成至少一轮短绳、中绳、长绳测试；
- 已完成低速、高速、下坠状态测试；
- 已测试连续换支点和高速重复投掷；
- 已记录是否出现无限加速、单一最优策略或明显关卡失效；
- 已把 A/B 结果和参数写入 `docs/DEVELOPMENT.md`；
- README 已明确当前处于“第二阶段测试”；
- 如果环境无法进入 Unity Play Mode，明确标注“未实际试玩验证”，不得自行宣布第二阶段完成。

第二阶段最终允许的结论只有：

- 保留；
- 调整后继续测试；
- 放弃。

在完成对比试玩前，不得预设结论。

---

## 15. Agent 行为准则

你是实现和验证这个物理原型的开发 Agent，不是替项目无限扩需求的产品经理。

遇到不确定设计时，优先选择：

> 更简单、限制更少、方便试玩验证的方案。

不要主动“完善”尚未验证的系统。

如果发现一个奇怪但有趣的物理行为：

> 先记录，后判断。

如果发现一个会改变玩法方向的重要问题：

> 在文档中明确写出，不要悄悄用补丁掩盖。

项目当前最重要的问题是：

> “在第一阶段移动基线之上，让固定后的悠悠球提供有限角加速度，究竟是在增加有趣的运动控制，还是只是在无脑放大机动性？”

第二阶段所有实现都应服务于这个对比测试。不要把实验机制悄悄变成正式机制，也不要为了证明某个结论而调参。
